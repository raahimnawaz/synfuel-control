"""Phase 6b: the edge-side bridge that closes the loop in software (SIL).

Routes telemetry from the (virtual or real) ESP32 node through the edge inference engine
and controller, and sends commands back -- the same JSON protocol a real Wokwi/hardware
ESP32 would speak.
"""
